[네트워크에 대한 기본 개념]

- 네트워크 : 서로 다른 디바이스가 연결되어 데이터를 주고받고 옮기는 것

- ip (Internet Protocol) -> '프로토콜'이란? 통신 규약. 네트워킹을 위한 규칙 같은 것.
    즉, ip는 목적지로 데이터를 전송하기 위해서 중간에 어떤 경로를 거쳐갈 것인지 정해주는 프로토콜

- TCP( Internet Protocol ) & UDP( User Datagram Protocol )
    TCP는 상대가 데이터를 제대로 받았는지 체크해주는 용도, UDP는 그냥 보내기만 할 뿐.

- 패킷 : 데이터 전송을 위해 데이터를 쪼갠 조각들

=> TCP/IP를 패킷 형태로 전송


[URL 클래스]
인터넷에 존재하는 서버들의 자원에 접근할 수 있는 주소

- 프로토콜 : 자원에 접근하기 위해 서버와 통신하는데 사용되는 통신 규약
    => http (hyper text transfer protocol) : hyper text를 전달해주는 매개체
- 체스트명 : 자원을 제공하는 서버의 이름
- 포트 번호 : 통신에 사용되는 서버의 포트 번호(ex: 네이버는 80)
- 경로명 : 접근하려는 자원이 저장된 서버상의 위치
- 파일명 : 접근하려는 자원의 이름
- 쿼리 :  URL 에서 ? 이후의 부분

[소켓 socket]
